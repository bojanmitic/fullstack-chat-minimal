/**
 * Authentication status enum
 * Matches NextAuth's session status values
 */
export enum AuthStatus {
  LOADING = "loading",
  AUTHENTICATED = "authenticated",
  UNAUTHENTICATED = "unauthenticated",
}
